let readlineSync = require('readline-sync');

function genGrid(grid1, grid2) {
  let grid = [];
  for(let i = 0; i < grid1; i++) {
    grid.push([]);
    for(let j = 0; j < grid2; j++) {
      grid[i].push("o");
    }
  } return grid;
}



module.exports = genGrid;
